
message(STATUS "----------------------------------------------add_dependencies(target1 target2)----------------------------------------------------------------------")
# add_dependencies(target1 target2)
# add_dependencies 用于指定一个目标（target）依赖于另一个目标。
# 这意味着在构建第一个目标之前，必须先构建第二个目标。
# target1: 需要依赖其他目标的目标。
# target2: target1 所依赖的目标。
message(STATUS "----------------------------------------------add_dependencies(target1 target2)----------------------------------------------------------------------\n")
